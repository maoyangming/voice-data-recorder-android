name: voice_data_recorder
description: 语音数字记录工具 - 实验室数据录入助手
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^10.4.5
  path_provider: ^2.1.3
  share_plus: ^10.0.0
  csv: ^6.0.0
  intl: ^0.19.0
  vosk_flutter: ^0.3.0
  record: ^5.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/vosk-model-small-cn-0.22.zip
